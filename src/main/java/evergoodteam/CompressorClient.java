package evergoodteam;


import net.fabricmc.api.ClientModInitializer;
import net.fabricmc.fabric.api.blockrenderlayer.v1.BlockRenderLayerMap;
import net.minecraft.client.render.RenderLayer;


public class CompressorClient implements ClientModInitializer {

    public void onInitializeClient() {
        /*
        Compressor.LOGGER.info("Initializing Client-side");

        for(int i = 0; i<Compressor.BLOCKS.size(); i++){
            BlockRenderLayerMap.INSTANCE.putBlock(Compressor.BLOCKS.get(i), RenderLayer.getTranslucent());
        }
        */
    }


}
